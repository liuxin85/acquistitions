services:
  api:
    build: .
    env_file:
      - .env
    environment:
      NODE_ENV: production
      ARCJET_ENV: development
      PORT: 3000
    ports:
      - '${HOST_PORT:-3001}:3000'
    restart: unless-stopped
